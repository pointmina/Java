package chap_02;

public class _02_Quiz {
    public static void main(String[] args) {

        int height = 111;
        int minimum_height = 120;
        String result = (height >= minimum_height) ? ("키가 " + height + "cm 이므로 탑승 가능합니다."): ("키가 " + height + "cm 이므로 탑승 불가능합니다.");
        System.out.println(result);



    }
}
